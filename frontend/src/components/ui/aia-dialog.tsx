'use client';

import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
} from '@mui/material';

export const AiaDialog = Dialog;
export const AiaDialogTitle = DialogTitle;
export const AiaDialogContent = DialogContent;
export const AiaDialogActions = DialogActions;
