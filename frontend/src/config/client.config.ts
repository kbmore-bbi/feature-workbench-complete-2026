import type { ClientConfig } from "@/types/client-config";
import { BBI_CONFIG } from "./bbi.config";
import { FOCUS_CONFIG } from "./focus.config";

// export const CLIENT_CONFIG: ClientConfig = BBI_CONFIG;
export const CLIENT_CONFIG: ClientConfig = FOCUS_CONFIG;
