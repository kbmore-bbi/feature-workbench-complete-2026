import { CLIENT_CONFIG } from '@/config/client.config';

/** Active client sidebar nav tokens (app sidebar + STTM section headers). */
export const SIDEBAR_NAV_TOKENS = CLIENT_CONFIG.theme.layout.sidebarNav;
