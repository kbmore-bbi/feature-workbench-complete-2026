"use client";

/**
 * @deprecated Use the global AppShell via AppShellGate in app/layout.tsx instead.
 */
export default function DashboardShell({ children }: { children: React.ReactNode }) {
  return children;
}
